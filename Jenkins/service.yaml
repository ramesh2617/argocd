apiVersion: v1
kind: Service
metadata:
  name: Jenkins-service
  namespace: jenkins
spec:
  type: NodePort
  selector:
    app: Jenkins
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30008
